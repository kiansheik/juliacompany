# Evidence Policy

Use four evidence levels:

- `high`: parsed source XML, explicit portal receipt text, or bank/payment proof.
- `medium`: PDF-only source where text extraction is partial but filename/context supports the record.
- `low`: filename/path inference without readable contents.
- `unknown`: fact not established.

Do not fabricate unknown metadata. If documents conflict, record the discrepancy in private state and generated reports.
